<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/menu_previous_week"
          android:title="@string/menu_previous_week"
          android:showAsAction="always"
          android:icon="@drawable/ic_back_button"
          android:onClick="showPreviousWeekStatistics" />

    <item android:id="@+id/menu_next_week"
          android:title="@string/menu_next_week"
          android:icon="@drawable/ic_next_button"
          android:showAsAction="always"
          android:onClick="showNextWeekStatistics" />
</menu>